# Where Does All The Data Come From? 🤔

## TL;DR: It's ALL Simulated/Fake Data!

Everything in this app is **simulated** or **hardcoded**. There are NO real sensors, NO real space settlement, and NO real data connections.

---

## 1. 📊 Sensor Data (Charts on Dashboard)

**Source:** `api/app/sensor_simulator.py`

**How it works:**
- The `SensorSimulator` class generates **fake random numbers** every second
- It uses Python's `random.gauss()` to create realistic-looking variations
- Example: Oxygen is always around 21% ± 0.5% (randomly varies)

```python
# This is FAKE data - just random numbers!
self.current_state.atmosphere.oxygen_level = max(19.0, min(23.0, 
    21.0 + random.gauss(0, 0.3)))  # Random number between 19-23%
```

**What you see:**
- O2 chart: Random numbers between 19-23%
- CO2 chart: Random numbers between 300-600 ppm
- Power chart: Random numbers between 0-1100 kW
- Battery chart: Random numbers between 60-100%
- Radiation chart: Random numbers around 0.02 mSv/hr

**It's NOT:**
- ❌ Connected to real sensors
- ❌ Reading from a database
- ❌ Getting data from NASA
- ❌ Real space settlement data

**It IS:**
- ✅ Computer-generated random numbers
- ✅ Designed to look realistic
- ✅ Updates every second automatically

---

## 2. 📚 Space Settlement Knowledge (Chatbot)

**Source:** `app/lib/knowledge-base.ts`

**How it works:**
- I created this based on **YOUR table of contents** that you provided
- All the information is **hardcoded** in the file
- When you ask about "radiation shielding", it looks up the hardcoded answer

**Example:**
```typescript
shielding: `Multi-layer radiation shielding: 3-5 meters regolith covering...`
```

**What you see:**
- Chatbot answers about HELIOS settlement
- Information about habitats, life support, etc.
- Based on the table of contents structure you gave me

**It's NOT:**
- ❌ Connected to real research papers
- ❌ Using a real database
- ❌ Getting info from the internet

**It IS:**
- ✅ Text I wrote based on your requirements
- ✅ Stored in a TypeScript file
- ✅ Can be edited/updated by you

---

## 3. 🚨 Alerts & Recommendations

**Source:** Generated by rules/thresholds in code

**How it works:**
- **Safety Layer** (`api/app/safety_layer.py`): Checks if values exceed thresholds
  - Example: If pressure drops >2% per minute → Alert!
- **Decision Engine** (`api/app/decision_engine.py`): Compares current values to optimal ranges
  - Example: If O2 < 20% → Recommend increasing O2
- **Anomaly Detector** (`api/app/anomaly_detector.py`): Uses statistics to find outliers
  - Example: If radiation is 3 standard deviations above normal → Alert!

**What you see:**
- Alerts when simulated values go out of range
- Recommendations based on simulated sensor readings
- All generated automatically by code rules

**It's NOT:**
- ❌ Real alerts from real systems
- ❌ Human-written alerts

**It IS:**
- ✅ Code checking if fake numbers exceed thresholds
- ✅ Automatic generation based on rules

---

## 4. 📈 Dashboard Charts

**Source:** Dashboard fetches from FastAPI, which gets data from SensorSimulator

**Data Flow:**
```
SensorSimulator (generates fake numbers)
    ↓
FastAPI /api/settlement-state endpoint
    ↓
Next.js Dashboard (fetches every 2 seconds)
    ↓
Recharts displays on screen
```

**What you see:**
- Live-updating charts
- Real-time data (updates every 2 seconds)

**It's NOT:**
- ❌ Real-time data from real sensors
- ❌ Historical data from a database

**It IS:**
- ✅ Fake data that updates in real-time
- ✅ Looks like a real monitoring system

---

## 5. 🎭 Scenarios (Radiation Storm, Pressure Leak)

**Source:** `api/app/scenario_engine.py`

**How it works:**
- When you trigger a scenario, it **overrides** the normal random generation
- Radiation storm: Forces radiation to spike to 2.0 mSv/hr
- Pressure leak: Forces pressure to drop at 2% per minute
- After scenario ends, goes back to normal random generation

**What you see:**
- Dramatic changes in sensor readings
- Alerts triggered by the scenario
- Recommendations generated

**It's NOT:**
- ❌ Real emergencies
- ❌ Real sensor failures

**It IS:**
- ✅ Scripted events that modify fake data
- ✅ Useful for testing alert systems

---

## Summary: Where Everything Comes From

| Component | Source | Real or Fake? |
|-----------|--------|---------------|
| Sensor readings (O2, CO2, etc.) | `sensor_simulator.py` - Random numbers | 🎲 **FAKE** |
| Space settlement info | `knowledge-base.ts` - Hardcoded text | 📝 **HARDCODED** |
| Alerts | Rules checking fake data | 🤖 **AUTO-GENERATED** |
| Recommendations | Rules comparing fake data to thresholds | 🤖 **AUTO-GENERATED** |
| Charts | Displaying fake data | 📊 **FAKE DATA** |
| Scenarios | Scripted modifications to fake data | 🎭 **SIMULATED** |

---

## Why Is It All Fake?

This is a **demo/prototype** project! It's designed to:
- ✅ Show how a real system would work
- ✅ Test alert and recommendation systems
- ✅ Demonstrate the UI/UX
- ✅ Be runnable without real hardware

**To make it real, you would need:**
- Real sensors connected via API
- Real database with historical data
- Real space settlement (obviously!)
- Real-time data feeds

---

## Can You Connect Real Data?

**YES!** You can replace the fake data sources:

1. **Replace SensorSimulator:**
   - Instead of `random.gauss()`, call a real sensor API
   - Example: `response = requests.get('https://real-sensor-api.com/telemetry')`

2. **Replace Knowledge Base:**
   - Connect to a real database
   - Use OpenAI API with real documents
   - Fetch from research papers API

3. **Keep the rest:**
   - Alerts, recommendations, dashboard all work the same
   - They just use real data instead of fake data

---

## Bottom Line

**Everything is simulated for demonstration purposes.** The app shows what a real system would look like, but uses fake data so you can run it on your computer without needing actual space settlement hardware! 🚀
